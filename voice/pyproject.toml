[project]
name = "voice"
version = "0.1.0"
description = "Gemini 3.1 Flash Live bridge for the governance voice companion"
requires-python = ">=3.11"
dependencies = [
    "google-genai>=1.73.1",
    "pypdf>=6.10.2",
    "python-dotenv>=1.2.2",
    "websockets>=16.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
    "ruff>=0.15.11",
]
